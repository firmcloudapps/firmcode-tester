# Firmcode Docker Runtime

Task 0.1 creates the Docker runtime folder so Docker Compose and service Dockerfiles can land here in Task 0.2.

Expected future files:

- `docker-compose.yml`
- `api.Dockerfile`
- `web.Dockerfile`
- `worker.Dockerfile`
